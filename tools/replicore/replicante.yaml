api:
  bind: '0.0.0.0:16016'


discovery:
  interval: 15
  backends:
    files:
      - 'conf.d/kafka.yaml'
      - 'conf.d/mongo.yaml'
      - 'conf.d/zookeeper.yaml'


logging:
  level: 'debug'


storage:
  backend: mongodb
  options:
    uri: mongodb://mongo:27017/
